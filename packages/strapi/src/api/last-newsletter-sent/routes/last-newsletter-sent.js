'use strict';

/**
 * last-newsletter-sent router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::last-newsletter-sent.last-newsletter-sent');
